# 调用配置中心的配置，bootstrap配置是spring默认的第一配置

#spring:
#  application:
#    name: blog-index
#  profiles:
#    active: dev
#  # 配置中心存放配置文件格式：${application.name}-${profiles.active}.myl
#  # 例如static-configer-dev.yml、static-configer-qas.yml
#  # 通过上述两个配置去配置中心读取对应的配置文件
#  cloud:
#    config:
#      # uri 配置中心地址
#      uri: http://localhost:1111
#      fail-fast: true